export default describe("Todos Page", () => {
  it("removes an item from the DOM when clicked", () => {
    cy.visit("/todos");

    // TODO: make some assertions/tests that prove the page works as expected
  });
});
